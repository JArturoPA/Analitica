'use strict'

angular.module('metlifeApp.directives',[]);

/*angular.module('metlifeApp.directives').directive('appVersion',['version',function(version){
	return {
		restrict: 'AE',
		link: function(scope,elem,attrs){
			elem.html(version);
		}
	}	
}]);*/